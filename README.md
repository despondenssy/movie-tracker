# 🎬 Cinema Tracker

Web-приложение для отслеживания фильмов и сериалов с персонализированными рекомендациями.

## О проекте

Cinema Tracker — это сервис для управления личной коллекцией фильмов и сериалов. Пользователи могут вести списки просмотра, ставить оценки, писать отзывы и получать персонализированные рекомендации на основе своих предпочтений.

### Основные возможности

- Регистрация и аутентификация пользователей
- Поиск фильмов и сериалов через TMDB API
- Управление статусом просмотра (Planned, Watching, Watched, Dropped)
- Система рейтинга (1-10) и текстовые отзывы
- Персонализированные рекомендации на основе контент-фильтрации
- Статистика пользователя и история активности
- Актуальные подборки фильмов (Trending, Popular)

## Технологический стек

**Backend:**

- Python 3.12
- Django 5.0
- PostgreSQL
- TMDB API

**Frontend:**

- HTML5, CSS3, JavaScript
- Bootstrap 5
- Custom CSS с адаптивным дизайном

**Дополнительно:**

- DiceBear API для генерации аватаров
- python-dotenv для управления переменными окружения

## Установка и запуск

### Требования

- Python 3.12+
- PostgreSQL 14+
- Git

### Шаги установки

1. **Клонируйте репозиторий:**

```bash
git clone https://github.com/despondenssy/movie-tracker.git
cd movie-tracker
```

1. **Создайте виртуальное окружение:**

```bash
python -m venv venv
source venv/bin/activate  # Для Windows: venv\Scripts\activate
```

1. **Установите зависимости:**

```bash
pip install -r requirements.txt
```

1. **Настройте переменные окружения:**

Создайте файл `.env` в корне проекта:

```env
SECRET_KEY=your-random-secret-key-here
DEBUG=True
TMDB_API_KEY=your_tmdb_api_key_here
DATABASE_URL=postgresql://user:password@localhost:5432/cinema_tracker_db
```

**Где взять:**

- `SECRET_KEY`: сгенерируйте командой:
  ```bash
  python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
  ```
- `TMDB_API_KEY`: получите на [https://www.themoviedb.org/settings/api](https://www.themoviedb.org/settings/api)

1. **Создайте базу данных:**

```bash
createdb cinema_tracker_db
```

1. **Примените миграции:**

```bash
python manage.py migrate
```

1. **Запустите сервер:**

```bash
python manage.py runserver
```

Приложение будет доступно по адресу: [http://127.0.0.1:8000/](http://127.0.0.1:8000/)

## 🎯 Алгоритм рекомендаций

Система рекомендаций использует **content-based filtering**: анализирует фильмы с оценкой 8-10, запрашивает похожие через TMDB API, фильтрует уже просмотренные и возвращает топ-24 рекомендации по рейтингу.

---

**Примечание:** Для работы приложения требуется активный интернет-доступ для запросов к TMDB API